package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Deep Space & Cyberpunk Palette
val DeepVoid = Color(0xFF050505)       // Deepest Space Obsidian (Primary Background)
val SpaceCard = Color(0xFF0E0E10)      // Cybernetic container (Semi-transparent base)
val SpaceDeck = Color(0xFF16161B)      // Raised interface / buttons
val ElectricBlue = Color(0xFF2563EB)   // Primary Action / Tech pulse
val LaserCyan = Color(0xFF06B6D4)      // Holographic highlight & focus
val NeonPurple = Color(0xFFA855F7)     // Level up / Attribute stat secondary color
val HolographicTeal = Color(0xFF14B8A6)// Accent / Hydration / Mindfulness
val NeonGreen = Color(0xFF22C55E)      // Streak / Task Success
val NeonCrimson = Color(0xFFEF4444)    // Alerts / Stress indicator
val SolarAmber = Color(0xFFF59E0B)     // Level indicator / XP stars
val SoftWhite = Color(0xFFE0E0E0)      // Primary high-contrast readability text
val MutedSlate = Color(0xFF8E8E9F)     // Secondary data/detail text
val CyberGrid = Color(0x1A6B7280)      // Grid overlay or glowing borders (White/10 style)
